const Router = require('express')
const router = new Router()

router.post('/',) 
router.get('/',) 
router.get('/:id',) //Получаем ийди отдельного устройства после перехода на его страницу с инфо




module.exports = router